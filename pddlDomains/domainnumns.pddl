; A numeric version of the domain

(define (domain cube-world)
  (:requirements :typing :fluents)
  (:types position)

  (:functions

  )

  (:action move-left
    :parameters ()
    :precondition ()
    :effect ()
  )
)
